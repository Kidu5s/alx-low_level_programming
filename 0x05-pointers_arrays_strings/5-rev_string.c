#include "main.h"

/**
 * rev_string - reverses a string
 * @s: a pointer to string
 *
 * Return: Nothing
 */
void rev_string(char *s)
{
	int len, i;

	for (len = 0; s[len]; len++)
		;
	for (i = len; i >= 0; i--)
		;
}

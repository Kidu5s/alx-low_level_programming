#include <stdio.h>

/**
 * main - prints its name
 * @argv: the array
 * @argc: the number of elelment
 *
 * Return: Always 0.
 */
int main(__attribute__((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
